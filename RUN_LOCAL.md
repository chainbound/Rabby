Follow these steps:

1. install dependencies with `yarn`

2. build local development project with `yarn build:dev`, this should produce a
   `dist` folder

3. open your browser and navigate to `chrome://extensions/`

4. activate "developer mode" toggle on the top right

5. click on "Load unpacked"

6. select the `dist` folder
